#include "main.h"
/**
  *mul - a function that multiplies two integers
  *@a: The input as an integer
  *@b: the input as an integer
  *Return: Addition of the multipcation
  */

int mul(int a, int b)
{
	return (a * b);
}
